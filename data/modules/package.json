{
  "name": "intelliverse-x-nakama-modules",
  "version": "1.0.0",
  "description": "IntelliVerse-X Nakama server runtime with custom Hiro + Satori systems",
  "scripts": {
    "check:rpc-literals": "node scripts/check-rpc-literals.js",
    "prebuild": "npm run check:rpc-literals",
    "build": "npx tsc && node scripts/seed-video-quiz-catalog.js && node -e \"const fs=require('fs'),p=require('path');fs.readdirSync('.').filter(f=>f.endsWith('.lua')).forEach(f=>fs.copyFileSync(f,p.join('build',f)));\" && node postbuild.js && echo Build complete",
    "build:watch": "npx tsc --watch",
    "docs:rpc-index": "node scripts/generate-rpc-index.js",
    "eval:social": "node tests/social_zone_eval_suite.mjs",
    "test:seedq": "node tests/seedq_imageguess_test_suite.mjs",
    "test:quest-ab": "node tests/quest_ab_overlay_regression_test.mjs",
    "eval:quest-ab": "node tests/quest_ab_overlay_live_rpc.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "nakama-common": "https://github.com/heroiclabs/nakama-common"
  }
}
